let jwt,User;_1df‍.x([["default",()=>_1df‍.o]]);_1df‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_1df‍.w("../models/user",[["default",["User"],function(v){User=v}]]);


const jwtMiddleware = async (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if(!token) return next(); // 토큰이 없음
    try{
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id : decoded._id,
            username : decoded.username,
        };
        // 여기부터 다시 하기
        // 토큰의 남은 유효 기간이 3.5일 미만이면 재발급
        const now = Math.floor(Date.now() / 1000);
        if(decoded.exp - now < 60 * 60 * 24 * 3.5){
            const user = await User.findById(decoded._id);
            const token = user.generateToken();
            ctx.cookies.set('access_token', token, {
                maxAge : 100 * 60 * 60 * 24 * 7, // 7일
                httpOnly: true,
            });
        }
        _1df‍.g.console.log(decoded);
        return next();
    } catch (e){
        // 토큰 검증 실패
        return next();
    }
};

_1df‍.d(jwtMiddleware);